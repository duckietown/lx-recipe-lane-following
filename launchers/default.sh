#!/bin/bash

echo "This exercise is not meant to be run with `dts code workbench`. Instead, navigate to the `packages/dt-core` 
directory and run `dts devel build` and `dts devel run` as described in the README.md file of this package."
